#!/usr/bin/python3
from string import ascii_uppercase;
from sys import stdout; stdout.buffer.write(bytes(range(65, 91)) + b'\n')
#exec("from sys import stdout; stdout.write(ascii_uppercase + '\\n')")
